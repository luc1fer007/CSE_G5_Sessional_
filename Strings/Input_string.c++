#include <bits/stdc++.h>
using namespace std;
int main()
{
    string S, T;
    cin >> S;
    cin.ignore(); // to ignore new line
    getline(cin, T);
    cout << S << endl;
    cout << T << endl;
    return 0;
}
